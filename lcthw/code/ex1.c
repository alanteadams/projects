#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100;
    int boy = 200;

    // this is also a comment
    printf("You are %d miles away.\n", distance);
    printf("What are %d have to do?\n", boy);

    return 0;
}
